ResourceDefinition:
  type: object
  required:
    - id
    - name
  properties:
    id:
      type: string
    name:
      type: string

ResourceList:
  type: object
  required:
    - data
  properties:
    data:
      type: array
      items:
        $ref: './03-complex-parted-schemas.yaml#/ResourceDefinition'
